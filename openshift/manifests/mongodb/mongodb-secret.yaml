apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: my-first-model
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: admin
  MONGO_INITDB_ROOT_PASSWORD: changeme123  # Change this in production!
  MONGO_CONNECTION_STRING: mongodb://admin:changeme123@mongodb:27017/mcp_demo?authSource=admin

